var X = true;

// 示例1: 用于声明字面量对象的成员
var obj = {
  value: 100,
  foo: function() {
     //...   }
}

// 示例2: 用于声明标签
myLabel: {   // ... }

// 示例3: 声明case分支
switch (obj) { 
  case X : break;
  default: {
  }
}

// 示例4: 用于条件(三元)表达式
X ? 'yes' : 'no'
