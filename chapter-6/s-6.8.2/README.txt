NOTE 1:
===
在运行本章其它测试之前，你需要使用：
	> bash init.sh
来初始化本目录中的prepack-core。完整项目的目录可能占用200M以上的空间。

你可以使用：
	> bash run.sh
来检测该目录中的环境是否正常初始化。

另外，如果`init.sh`被异常中断（因此你需要重新初始化），那么你可以
使用`-f`参数再次运行：
	> bash init.sh -f