NOTE 1:
===
Babel的Parser模块可以作为独立模块使用，而无需通过npm安装。因此你可以使用：
	> node 1.js
来运行这个测试。

另外，你可以使用：
	> bash init.sh
来从`../s-6.8.2`中获取一个该模块的副本，或者你也可以独立下载一个它的安装包。
并且在任何情况下，可以使用`-f`参数再次获取：
	> bash init.sh -f

最后，test/index.js中的代码并不使用当前目录下的lib/@babel/parser。因
此当你使用mocha时它实际使用的仍然是`../s-6.8.2`中的原版本。